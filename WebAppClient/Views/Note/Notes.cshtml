@model IEnumerable<Note>

@{
    ViewBag.Title = "title";
    Layout = "_Layout";
}

<div class="d-flex flex-row justify-content-between mb-4">
    <span class="d-inline"><h2>All Notes</h2></span>
    @Html.ActionLink("Add Note", "AddNote", "Note", "",new { @class="btn btn-primary", role="button"})
</div>

<div class="container">
    <table class="table table-hover">
        <thead>
        <tr>
            <th scope="col">#</th>
            <th scope="col">Patient</th>
            <th scope="col">Doctor</th>
            <th scope="col">Disease</th>
            <th scope="col">Data</th>
        </tr>
        </thead>
        <tbody>
        @foreach (var note in this.Model)
        {
            <tr>
                <th scope="row">@note.Id</th>
                <td>@note.PatientId</td>
                <td>@note.DoctorId</td>
                <td>@note.DiseaseId</td>
                <td>@note.DateVisit.ToString()</td>
            </tr>
        }
        </tbody>
    </table>
</div>